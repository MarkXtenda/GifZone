function Search({search, rating}) {
  
    return (
        <section>
            Search
            <p>{search}</p>
            <p>{rating}</p>
        </section>
    );
  }
  
  export default Search;